## Requirements

You need a local installation of a TeX compiler. For MacOS, install MacTeX by running the following:
```bash
brew install --cask mactex-no-gui
```

## Compile

To compile, use the command:
```bash
./scripts/build.sh
```

If you want a VSCode extension, install Latex Workshop.